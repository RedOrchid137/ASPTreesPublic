@*@model IEnumerable<ASP.groep.L.Domain.QRCode>

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">QRCodes List</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="QRCode" asp-action="Create" class="btn btn-primary">Create new QRCode</a>
        </div>
    </div>
    <br />

    @if (Model.Count() > 0)
    {
        <table class="table table-bordered table-striped" style="width:100%">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>PicturePath</th>
                    <th>Zone</th>
                    <th>Changes</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var qrCode in Model)
                {
                    <tr>
                        <td width="10%">@qrCode.Id</td>
                        <td width="35%">@qrCode.PicturePath</td>
                        <td width="35%">@qrCode.Zone</td>
                        <td width="20%">
                            <div class="w-100 btn-group" role="group">
                                <a asp-controller="QRCode" asp-action="Update" asp-route-Id="@qrCode.Id" class="btn btn-primary mx-1">Update</a>
                                <a asp-controller="QRCode" asp-action="Delete" asp-route-Id="@qrCode.Id" class="btn btn-danger mx-1">Delete</a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <p>No QRCodes created yet.</p>
    }


</div>*@